# 桌面宠物 Skill · 使用说明（给最终用户）

把你的宠物做成 Windows 桌面上会动、会说话的小伙伴。**你只需要准备一张宠物照片。**

## 零安装：一条指令开工（推荐）

拿到 `desktop-pet-creator.zip` 后，**不用手动放任何文件夹**，直接在 WorkBuddy 对话里：

1. 把 `desktop-pet-creator.zip` 和宠物照片一起拖进对话；
2. 说一句：「**帮我安装这个桌宠 skill，然后用这张照片做一只桌宠**」；
3. AI 自动完成安装 + 制作 + 装桌面，你只需最后双击桌面的启动文件。

> 如果之前已经装过这个 skill，直接说「做个桌宠」并拖照片即可，zip 都不用再给。

### 连文件都不用传：从 git 直连安装（推荐分发方式）

把下面这条指令直接发给对方（对方不用接收任何文件），AI 会自己从网上把 skill 下载并装好：

> 「**帮我从这个链接安装桌宠 skill：https://github.com/Mio0707/desktop-pet-creator/releases/latest/download/desktop-pet-creator.zip**」

装好后照常：拖照片 → 说「做个桌宠」→ 双击桌面启动文件。

## 最省事的做法（推荐，零门槛）

1. **传照片**：在 WorkBuddy 对话里把宠物的照片拖进来，说一句「做个桌宠」。
2. **等一会儿**：AI 会自动按默认设置生成宠物，并直接装到你的桌面上。全程不用你回答任何问题、不用开任何页面。
3. **召唤宠物**：双击桌面上的「我的桌面宠物-桌宠.bat」，宠物出现在桌面；拖拽可移动，右键或按 ESC 关闭。
   - 若 Windows 提示是否允许运行脚本，选「允许」（启动后没有黑窗口）。

> 照片如果带复杂背景，宠物会连同背景一起显示在桌面。想变成透明背景，就说一句「帮我把背景去掉」即可。

## 想要更生动？

- **程序动画（已自带，0 额外成本）**：待机呼吸起伏、悬停歪头、单击弹跳、双击跳跃。
- **只给一张图也能做**：其余动作状态会复用这张图，宠物同样能跑、能说话。
- **多动作 / 帧动画 / 自己写每句台词**：让 AI 帮你打开一个制作台网页自己调（见进阶）。

## 进阶：自己做精细调整

当你想要更多动作状态、给每个互动单独写台词、或做挥手之类的小动画时，告诉 AI：

- 「帮我把宠物做出挥手、开心的动作」（AI 会用 AI 制图生成多个状态图，会先告诉你大概要花多少积分）
- 「打开制作台，我想自己配每句台词」
- AI 会弹出一个网页制作台：左边是所有透明状态图，右边给每个交互（悬停 / 单击 / 双击 / 休息）配对应的动作和台词，还能改名字、休息换图间隔。改完点「完成制作」，桌面会更新启动程序。

## 改了想更新？

直接说「把之前那个桌宠的点击台词改成 xx」或「换个 idle 图」，AI 只会改你点名的部分，名字、其他台词、间隔、旧图都原样保留，不用从头重做。

## 文件结构（生成的宠物包）

```
<名字>-桌宠.zip
├─ pet-desktop.ps1      # 桌宠运行时（PowerShell + WPF）
├─ start-pet.bat        # 启动器
├─ config.json          # 状态图、交互、台词配置（UTF-8）
├─ README.txt
└─ assets/              # 各状态的透明 PNG（多帧状态为 _1.png、_2.png…）
```

安装后还会在 `C:\Users\<你>\DesktopPets\<名字>\` 留一份，桌面上的「<名字>-桌宠.bat」就是它的启动快捷方式。

## 常见问题

- **宠物没出现**：确认双击的是桌面的「<名字>-桌宠.bat」；若启动后无反应，安装目录里有 `pet-error.log`，把它发来即可定位。
- **图片带方块背景**：零门槛模式不自动抠复杂背景；说一句「帮我把背景去掉」，AI 会帮你处理（纯色背景免费抠，复杂背景可能要重新生成透明版）。
- **想换电脑用**：把安装目录 `DesktopPets\<名字>\` 整个拷过去，再双击里面的 `start-pet.bat` 即可（目标机需 Windows + PowerShell 5.1+）。
