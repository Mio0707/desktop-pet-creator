# 桌面宠物 Skill · 使用说明（给最终用户）

把你的宠物做成 Windows 桌面上会动、会说话的小伙伴。**你只需要准备一张宠物照片。**

## 安装 skill（零文件操作）

不用手动放任何文件夹，三选一：

- **从链接安装（推荐）**：把下面这句话发给 AI，它会自己从网上下载并装好——
  「**帮我从这个链接安装桌宠 skill：https://github.com/Mio0707/desktop-pet-creator/releases/latest/download/desktop-pet-creator.zip**」
- **拖 zip 安装**：把 `desktop-pet-creator.zip` 拖进对话，说「帮我安装这个桌宠 skill」。
- **双击安装**：双击「安装桌宠skill.bat」。

> 已经装过的机器跳过本步。装好后，拖照片 + 一句话即可开工。

## 默认制作流程

1. **传照片**：在 WorkBuddy 对话里把宠物的真实照片拖进来，说一句「用桌宠 skill 帮我做桌面宠物」。
2. **AI 派生状态图 + 自动抠图**：AI 会基于你的照片生成多个状态图（默认 idle 待机 / rest 休息 / curious 好奇 / happy 开心 / excited 兴奋 / wave 打招呼，共 6 张），并自动抠成透明背景。
   - **生成前会先告诉你大概要花多少积分，你同意才开始**（每张约 5-10 积分，不想花太多可以只要 4 张基础款）。
   - 每张图出来都会给你过目，不满意的可以单张重做。
3. **打开制作台**：自动弹出一个网页制作台，左边是所有透明状态图（同一状态有多张的会显示成「帧序列」胶片条），右边给每个交互（悬停 / 单击 / 双击 / 休息）配置对应的状态和台词，还能改宠物名字、兜底台词、休息换图间隔。
4. **完成制作**：点「完成制作」，桌面会立刻生成「<名字>-桌宠.bat」启动程序，并安装到 `C:\Users\<你>\DesktopPets\<名字>\`。
5. **召唤宠物**：双击桌面的启动程序，宠物出现在桌面右下角；拖拽可移动，右键或按 ESC 关闭。若 Windows 提示是否允许运行脚本，选「允许」即可（启动后无黑窗口）。

> 制作台页面可以反复改、反复点「完成制作」，每次都会覆盖更新安装目录和桌面启动程序。

## 想要更生动？

- **程序动画（已自带，0 额外成本）**：待机呼吸起伏、悬停歪头、单击弹跳、双击跳跃。
- **帧序列动画（GIF 效果）**：让 AI 对某个状态生成 2~4 张连续画面（比如挥手的第 1/2/3 帧），制作台里该状态会显示为「N 帧」胶片条，运行时进入该状态就会连播成小动画。建议只对 1~2 个招牌状态做（每张帧都是一次生成、消耗积分）。
- **只想用一张原图**（不派生、省积分）：直接说「只用这张图做」，其余状态会自动复用这张图，宠物同样能跑、能说话。

## 改了想更新？

直接说「把之前那个桌宠的点击台词改成 xx」或「换个 idle 图」，AI 会用增量更新——只改你点名的部分，名字、其他台词、间隔、旧图都原样保留，不用从头重做。

## 文件结构（生成的宠物包）

```
<名字>-桌宠.zip
├─ pet-desktop.ps1      # 桌宠运行时（PowerShell + WPF）
├─ start-pet.bat        # 启动器
├─ config.json          # 状态图、交互、台词配置（UTF-8）
├─ README.txt
└─ assets/              # 各状态的透明 PNG（多帧状态为 _1.png、_2.png…）
```

安装后还会在 `C:\Users\<你>\DesktopPets\<名字>\` 留一份，桌面上的「<名字>-桌宠.bat」就是它的启动快捷方式。

## 常见问题

- **宠物没出现**：确认双击的是桌面的「<名字>-桌宠.bat」；若启动后无反应，安装目录里有 `pet-error.log`，把它发来即可定位。
- **图片带方块背景**：AI 生成图通常带浅色底；本 skill 已自动抠图成透明，若边缘不干净可让我重抠或换张生成图。
- **积分花超了**：默认 6 张约 30-60 积分；想省就只要 4 张基础款，或只用原图（不派生）。
- **想换电脑用**：把安装目录 `DesktopPets\<名字>\` 整个拷过去，再双击里面的 `start-pet.bat` 即可（目标机需 Windows + PowerShell 5.1+）。
